# 涨停二板主升浪因子挖掘项目方案

> 本文档聚焦一个具体的量化策略目标：**训练能够选出"首板涨停后、能够第二次涨停、随后走出主升浪"的股票**的因子模型。
>
> 阶段定位：**仅限研究回测，暂不涉及实盘执行。**

---

## 1. 策略核心逻辑

### 1.1 目标形态

```
股价
  │
  │                                    ╱ ╲
  │                                  ╱     ╲
  │                               ╱         ╲    主升浪
  │                            ╱             ╲  ╱
  │                         ╱                 ╱
  │                      ╱                   ↑
  │                   ╱                  第二次涨停
  │                ╱                    （时间间隔 T 内）
  │             ╱                       ↑
  │          ╱                       ╱
  │       ╱                        ╱
  │    ╱                         ╱
  │ ──────────────────────────────
  │         ↑
  │    首板涨停
  │
└─────────────────────────────────────────────→ 时间
```

### 1.2 三个关键节点

| 节点 | 定义 | 技术处理 |
|------|------|----------|
| **首板涨停** | 日涨幅 ≥ 9.9%（ST股除外） | 事件标记 |
| **二板涨停** | 首板后 N 个交易日内再次涨停 | 事件序列检测 |
| **主升浪** | 二板后 M 个交易日内涨幅达到阈值 | 区间收益计算 |

### 1.3 核心问题

> **本质是一个事件序列预测问题**：在首板涨停发生后，预测未来能否出现二次涨停，以及二次涨停后是否能走出主升浪。

---

## 2. 方案选型

### 2.1 为什么选 Qlib

| 需求 | Qlib 支持情况 |
|------|--------------|
| 自定义涨停事件因子 | ✅ 支持 Python 表达式因子 |
| 事件序列特征构建 | ✅ 可通过自定义 Handler 实现 |
| 机器学习模型 | ✅ LightGBM / GBDT / LSTM 等 |
| 回测验证 | ✅ 内置回测框架 |
| 日线数据 | ✅ 支持 |

### 2.2 是否需要 Kronos

| 场景 | 是否需要 Kronos |
|------|----------------|
| 先跑通基础逻辑 | 不需要，Qlib 足够 |
| 经典技术因子挖掘 | 不需要 |
| 想用 AI 挖掘隐含因子 | 需要，作为增强 |

**建议：先用 Qlib 自建因子体系跑通验证，有效果后再引入 Kronos 增强。**

---

## 3. 整体架构

```
原始行情数据（AKShare / QlibData）
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                    Qlib 数据层                           │
│              日线数据（含复权因子）                       │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                  事件定义层                              │
│     涨停事件标记 / 事件序列构建 / 样本筛选                 │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                  特征因子层                              │
│    量价因子 / 资金流因子 / 板块因子 / 情绪因子            │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                  标签定义层                              │
│    二板成功标签 / 主升浪标签 / 复合标签                   │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                  模型训练层                              │
│              LightGBM 二分类 / 多分类                     │
└─────────────────────────────────────────────────────────┘
         │
         ▼
         Qlib 回测验证
```

---

## 4. 样本与标签定义

### 4.1 样本定义

#### 正样本
首板涨停后 **5 个交易日**内出现二板，且二板后 **10 个交易日**涨幅达到 **15%**。

#### 负样本
首板涨停后 **5 个交易日内未出现二板**，或二板后未达到主升浪标准。

### 4.2 标签体系

#### 方案一：二板预测（二分类）

```python
# 首板后5日内是否出现二板
label_short = {
    "二板成功": 1,    # 5日内再次涨停
    "二板失败": 0,    # 5日内未二板
}
```

#### 方案二：复合标签（三分类，推荐）

```python
label_combined = {
    "完美标的": 2,     # 二板成功 且 主升浪成功（二板后10日涨幅 > 15%）
    "二板成功": 1,     # 二板成功 但 主升浪失败
    "二板失败": 0,     # 未二板
}
```

**推荐使用复合标签**，可以同时学到"能否二板"和"能否走主升浪"两个信息。

---

## 5. 特征因子体系

### 5.1 基础量价特征（在首板收盘时提取）

```python
feature_list = [
    # 收益类
    "returns(close, 1)",      # 昨日收益率
    "returns(close, 3)",      # 3日收益率
    "returns(close, 5)",      # 5日收益率
    "returns(close, 10)",     # 10日收益率
    "returns(close, 20)",     # 20日收益率

    # 量能类
    "volume / volume(rolling_5_mean)",  # 量比
    "volume / volume(rolling_10_mean)", # 10日均量比
    "turnover_rate",                  # 换手率

    # 均线类
    "close / close(rolling_20_min)",  # 相对20日均线位置
    "close / close(rolling_60_min)",  # 相对60日均线位置
    "close / close(rolling_120_min)", # 相对120日均线位置

    # 波动类
    "std(returns(close, 5))",   # 5日波动率
    "std(returns(close, 10))",  # 10日波动率
    "max(high, 5) / min(low, 5) - 1",  # 5日区间振幅
]
```

### 5.2 涨停相关特征（核心）

```python
limit_up_features = [
    # 首板当日特征
    "limit_up_amount",           # 涨停封单金额（万元）
    "limit_up_duration_ratio",  # 涨停持续时间占比（0~1）
    "turnover_rate",            # 换手率
    "amplitude",                # 当日振幅
    "open_minutes_to_limit",    # 从开盘到涨停的时间（分钟）

    # 首板前特征
    "consecutive_up_days",      # 连续上涨天数
    "avg_volume_5d",            # 5日均量
    "volume_ratio",             # 首板当日量比 = 成交量 / 5日均量
    "avg_limit_up_gap_5d",     # 5日内平均涨停间隔

    # 历史涨停统计
    "limit_up_count_20d",       # 20日内涨停次数
    "limit_up_count_60d",       # 60日内涨停次数
    "second_limit_up_count_20d", # 20日内二板次数
    "historical_second_ratio",  # 历史二板率（二板次数/涨停次数）

    # 强度指标
    "strength_1d",   # 首板力度 = (收盘价-开盘价)/涨停幅度
    "strength_3d",   # 3日力度 = (收盘价-3日前收盘)/3日前涨停幅度
]
```

### 5.3 板块与情绪特征

```python
sector_features = [
    "sector_leading_strength",   # 板块龙头强度
    "sector_avg_return",         # 板块平均涨幅
    "market_up_count",           # 当日上涨股票数量
    "market_limit_up_count",     # 当日涨停股数量（情绪指标）
    "market_turnover_rate",       # 市场整体换手率
    "market_sentiment",          # 市场情绪 = 上涨家数 / (上涨+下跌)
]
```

### 5.4 资金流特征（可选）

```python
money_flow_features = [
    "主力资金净流入",             # 超大单+大单净流入（万元）
    "主力资金流入占比",           # 主力净流入/总成交额
    "融资余额变化率",             # 融资余额变化率
]
```

### 5.5 涨停前走势特征（核心，2个月窗口）

> 涨停前的走势特征是预测二板成功与否的关键因素。强势股在涨停前往往有吸筹、洗盘等痕迹。

#### 5.5.1 趋势类特征

```python
pre_trend_features = [
    # 累计收益率（从涨停前N日开始）
    "return_1d",      # 涨停前1日收益率
    "return_3d",      # 涨停前3日收益率
    "return_5d",      # 涨停前5日收益率
    "return_10d",     # 涨停前10日收益率
    "return_20d",     # 涨停前20日收益率（1个月）
    "return_40d",     # 涨停前40日收益率（2个月）

    # 均线多头排列
    "ma5_above_ma10",      # 5日均线 > 10日均线
    "ma10_above_ma20",     # 10日均线 > 20日均线
    "ma20_above_ma60",     # 20日均线 > 60日均线
    "ma多头排列得分",       # 均线多头排列强度（0~3）

    # 趋势强度
    "slope_20d",           # 20日线斜率
    "slope_60d",           # 60日线斜率
    "trend_strength_20d",  # 20日趋势强度 = (C0 - MA20) / MA20 / std(return)
]
```

#### 5.5.2 量价配合特征

```python
pre_volume_price_features = [
    # 量增价涨/跌背离
    "volume_change_20d",   # 涨停前20日成交量变化率
    "price_change_20d",   # 涨停前20日价格变化率
    "volume_price_corr",  # 量价相关性（20日）

    # 缩量洗盘
    "volume_ratio_40d_avg",     # 40日均量比（涨停前量能萎缩程度）
    "volume_convergence_20d",   # 20日量能收敛程度（缩量洗盘信号）

    # 异动检测
    "volume_spike_count_40d",   # 40日内放量异动次数
    "price_gap_count_40d",     # 40日内跳空次数
]
```

#### 5.5.3 波动类特征

```python
pre_volatility_features = [
    # 振幅变化
    "amplitude_20d_avg",    # 涨停前20日平均振幅
    "amplitude_40d_avg",    # 涨停前40日平均振幅
    "amplitude_trend",      # 振幅趋势（放大or收缩）

    # 波动率
    "volatility_20d",       # 涨停前20日波动率
    "volatility_40d",       # 涨停前40日波动率
    "volatility_ratio",     # 短期/长期波动率比

    # 横盘整理
    "consolidation_days_20d",   # 涨停前20日内横盘天数（振幅<3%）
    "consolidation_days_40d",   # 涨停前40日内横盘天数
]
```

#### 5.5.4 支撑压力特征

```python
pre_support_resistance_features = [
    # 涨停前价格位置
    "price_position_20d",     # 相对20日高低点位置（0~1）
    "price_position_60d",      # 相对60日高低点位置（0~1）
    "near_20d_high",          # 距离20日高点距离（%）
    "near_60d_high",          # 距离60日高点距离（%）

    # 筹码分布（估算）
    "cost_basis_20d",         # 20日筹码密集区成本
    "cost_basis_60d",        # 60日筹码密集区成本
    "above_cost_basis_pct",   # 当前价高于成本均线的比例

    # 压力位突破
    "break_20d_high_count",   # 涨停前20日突破20日高点次数
    "break_60d_high_count",   # 涨停前20日突破60日高点次数
]
```

#### 5.5.5 资金流向特征

```python
pre_money_flow_features = [
    # 资金净流入（估算）
    "net_inflow_5d",          # 涨停前5日主力净流入
    "net_inflow_10d",         # 涨停前10日主力净流入
    "net_inflow_20d",         # 涨停前20日主力净流入

    # 资金趋势
    "inflow_trend_10d",       # 10日资金趋势（流入加速or减速）
    "inflow_acceleration",    # 资金流入加速度

    # 换手率变化
    "turnover_change_20d",    # 涨停前20日换手率变化
    "avg_turnover_20d",       # 涨停前20日平均换手率
]
```

#### 5.5.6 形态识别特征

```python
pre_pattern_features = [
    # K线形态
    "实体占比_20d_avg",       # 涨停前20日平均实体占比（判断活跃度）
    "上影线占比_20d_avg",    # 涨停前20日平均上影线比例

    # 经典形态
    "形态_老鸭头",            # 是否满足老鸭头形态
    "形态_旗形整理",          # 是否处于旗形整理
    "形态_收敛三角",          # 是否收敛三角末端

    # 突破信号
    "放量突破20d_high",      # 放量突破20日高点
    "放量突破60d_high",      # 放量突破60日高点
    "缩量回踩均线",           # 缩量回踩均线后企稳
]
```

#### 5.5.7 涨停前走势综合评分

```python
pre_trend_score = {
    # 趋势得分（0~100）
    "趋势得分": (
        "均线多头排列得分" * 0.3 +
        "return_20d_rank" * 0.3 +      # 20日涨幅在全校友排
        "slope_20d_normalized" * 0.2 + # 20日线斜率标准化
        "trend_strength_20d" * 0.2     # 趋势强度
    ),

    # 量能得分（0~100）
    "量能得分": (
        "volume_ratio_40d_avg" * 0.4 +  # 缩量洗盘程度
        "net_inflow_20d_rank" * 0.3 +    # 资金流入排名
        "volume_spike_count_40d" * 0.3  # 异动次数
    ),

    # 形态得分（0~100）
    "形态得分": (
        "consolidation_days_40d_norm" * 0.4 + # 横盘整理程度
        "cost_basis_alignment" * 0.3 +        # 筹码分布
        "pattern_score" * 0.3                  # 形态识别
    ),

    # 综合得分
    "涨停前综合得分": (
        "趋势得分" * 0.4 +
        "量能得分" * 0.3 +
        "形态得分" * 0.3
    )
}
```

---

## 6. 数据获取

### 6.1 数据来源

| 来源 | 说明 | 推荐度 |
|------|------|--------|
| **QlibData** | 内置日线数据，开箱即用 | ⭐⭐⭐⭐⭐ |
| **AKShare** | 免费财经数据接口 | ⭐⭐⭐⭐ |
| **Tushare** | 需要积分 | ⭐⭐⭐ |

### 6.2 Qlib 数据初始化

```bash
# 安装
pip install pyqlib

# 下载 A 股日线数据（约 5GB）
python -m qlib.run.get_data --target_dir ./qlib_data/cn_data --region cn
```

### 6.3 最小数据字段

如果自行接入数据，至少需要：

```python
required_fields = [
    "date",           # 日期
    "code",           # 股票代码
    "open",           # 开盘价
    "high",           # 最高价
    "low",            # 最低价
    "close",          # 收盘价
    "volume",         # 成交量
    "turnover_rate",  # 换手率
    "change_pct",     # 涨跌幅
]
```

---

## 7. 模型训练

### 7.1 数据划分

#### 方案一：时序划分（默认）

按时间顺序划分，模拟真实预测场景：

```python
segments = {
    "train": ("2018-01-01", "2020-12-31"),  # 训练集
    "valid": ("2021-01-01", "2021-06-30"), # 验证集
    "test":  ("2021-07-01", "2023-12-31"), # 测试集
}
```

**优点**：符合实际预测场景（用历史预测未来）
**缺点**：可能有市场风格漂移问题

---

#### 方案二：板块股票划分（推荐用于多板块策略）

按板块和股票划分，验证策略在不同板块/股票上的泛化能力：

```python
from sklearn.model_selection import GroupKFold, StratifiedGroupKFold

class SectorStockSplit:
    """按板块和股票划分训练集、验证集、测试集"""

    def __init__(self,
                 n_splits: int = 5,
                 test_ratio: float = 0.2,
                 valid_ratio: float = 0.1):
        """
        Args:
            n_splits: 用于交叉验证的折数
            test_ratio: 测试集比例
            valid_ratio: 验证集比例
        """
        self.n_splits = n_splits
        self.test_ratio = test_ratio
        self.valid_ratio = valid_ratio

    def split_by_sector(self,
                        df: pd.DataFrame,
                        sector_col: str = "sector",
                        stock_col: str = "code",
                        label_col: str = "label_combined") -> dict:
        """
        按板块划分数据

        策略：将部分板块作为测试集，其余板块作为训练集
        验证策略在不同板块上的泛化能力

        Args:
            df: 包含 sector, code, label 的数据
            sector_col: 板块列名
            stock_col: 股票代码列名
            label_col: 标签列名

        Returns:
            dict: {
                "train": DataFrame,
                "valid": DataFrame,
                "test": DataFrame
            }
        """
        sectors = df[sector_col].unique()
        np.random.seed(42)
        np.random.shuffle(sectors)

        n_sectors = len(sectors)
        n_test = int(n_sectors * self.test_ratio)
        n_valid = int(n_sectors * self.valid_ratio)

        test_sectors = sectors[:n_test]
        valid_sectors = sectors[n_test:n_test + n_valid]
        train_sectors = sectors[n_test + n_valid:]

        return {
            "train": df[df[sector_col].isin(train_sectors)],
            "valid": df[df[sector_col].isin(valid_sectors)],
            "test": df[df[sector_col].isin(test_sectors)],
        }

    def split_by_stock(self,
                       df: pd.DataFrame,
                       stock_col: str = "code",
                       label_col: str = "label_combined") -> dict:
        """
        按股票划分数据

        策略：将部分股票作为测试集，其余作为训练集
        验证策略对新股票的预测能力（模拟选股场景）

        Args:
            df: 包含 code, label 的数据
            stock_col: 股票代码列名
            label_col: 标签列名

        Returns:
            dict: {
                "train": DataFrame,
                "valid": DataFrame,
                "test": DataFrame
            }
        """
        stocks = df[stock_col].unique()
        np.random.seed(42)
        np.random.shuffle(stocks)

        n_stocks = len(stocks)
        n_test = int(n_stocks * self.test_ratio)
        n_valid = int(n_stocks * self.valid_ratio)

        test_stocks = stocks[:n_test]
        valid_stocks = stocks[n_test:n_test + n_valid]
        train_stocks = stocks[n_test + n_valid:]

        return {
            "train": df[df[stock_col].isin(train_stocks)],
            "valid": df[df[stock_col].isin(valid_stocks)],
            "test": df[df[stock_col].isin(test_stocks)],
        }

    def split_by_sector_and_stock(self,
                                  df: pd.DataFrame,
                                  sector_col: str = "sector",
                                  stock_col: str = "code",
                                  label_col: str = "label_combined") -> dict:
        """
        按板块和股票联合划分

        策略：
        - 训练集：部分板块的所有股票
        - 验证集：部分板块的部分股票
        - 测试集：剩余板块的所有股票

        最严格的划分方式，验证板块间泛化能力

        Args:
            df: 包含 sector, code, label 的数据
            sector_col: 板块列名
            stock_col: 股票代码列名
            label_col: 标签列名

        Returns:
            dict: {
                "train": DataFrame,
                "valid": DataFrame,
                "test": DataFrame
            }
        """
        sectors = df[sector_col].unique()
        np.random.seed(42)
        np.random.shuffle(sectors)

        n_sectors = len(sectors)
        n_test = int(n_sectors * self.test_ratio)
        n_valid = int(n_sectors * self.valid_ratio)

        test_sectors = sectors[:n_test]
        valid_sectors = sectors[n_test:n_test + n_valid]
        train_sectors = sectors[n_test + n_valid:]

        # 验证集：从训练板块中抽取部分股票
        train_df = df[df[sector_col].isin(train_sectors)]
        train_stocks = train_df[stock_col].unique()
        np.random.seed(123)
        np.random.shuffle(train_stocks)
        n_valid_stocks = int(len(train_stocks) * self.valid_ratio / (1 - self.test_ratio))
        valid_stocks = train_stocks[:n_valid_stocks]

        return {
            "train": train_df[~train_df[stock_col].isin(valid_stocks)],
            "valid": df[df[stock_col].isin(valid_stocks)],
            "test": df[df[sector_col].isin(test_sectors)],
        }

    def cross_validate(self,
                      df: pd.DataFrame,
                      group_col: str,
                      n_splits: int = 5) -> Iterator[dict]:
        """
        交叉验证划分

        使用 GroupKFold，确保同一 group（板块/股票）不会同时出现在训练集和测试集

        Args:
            df: 数据
            group_col: 分组列（sector 或 code）
            n_splits: 折数

        Yields:
            Iterator of {"train": DataFrame, "test": DataFrame}
        """
        gkf = GroupKFold(n_splits=n_splits)
        groups = df[group_col].values

        for train_idx, test_idx in gkf.split(df, groups=groups):
            yield {
                "train": df.iloc[train_idx],
                "test": df.iloc[test_idx],
            }
```

**板块划分的实际意义**：
- 如果策略在"科技板块"上训练，在"消费板块"上测试效果差，说明策略可能过度拟合了特定板块的市场特征
- 这种划分能更真实地反映策略的泛化能力

**股票划分的实际意义**：
- 如果策略在历史股票上训练，对新上市股票预测效果好，说明策略学到了通用的选股逻辑
- 模拟"每日选股"的真实场景

### 7.2 模型配置

```python
from qlib.contrib.model.gbdt import LGBModel

model_config = {
    "loss": "multi_logloss",     # 多分类
    "num_class": 3,              # 三分类
    "objective": "multiclass",
    "num_leaves": 31,
    "learning_rate": 0.05,
    "feature_fraction": 0.8,
    "bagging_fraction": 0.8,
    "bagging_freq": 5,
    "verbose": -1,
}
```

### 7.3 评估指标

| 指标 | 说明 | 目标 |
|------|------|------|
| **IC** | 信息系数，预测与收益相关性 | > 0.03 |
| **IR** | 信息比 = IC均值/IC标准差 | > 0.5 |
| **AUC** | 区分度 | > 0.6 |
| **Accuracy** | 准确率 | - |
| **Precision/Recall** | 精确率/召回率 | 平衡 |

---

## 8. 回测验证

### 8.1 回测规则

| 项目 | 规则 |
|------|------|
| 买入时机 | 首板次日开盘买入（模拟涨停后次日接力） |
| 卖出时机 | 二板次日开盘 / 固定 N 日后 / 止损线 |
| 仓位 | 单只股票仓位上限 20% |
| 基准 | 沪深300（SH000300） |
| 滑点 | 0.3% |

### 8.2 回测框架

```python
from qlib.contrib.evaluate import backtest_daily

backtest_config = {
    "start_date": "2022-01-01",
    "end_date": "2023-12-31",
    "account": 10000000,       # 1000万模拟账户
    "benchmark": "SH000300",
    "strategy": {
        "topk": 10,            # 持仓最多10只
        "Sell_n": 5,           # 持仓超过5日强制卖出
        "signal_method": "model_output",
    }
}

report = backtest_daily(**backtest_config)
```

### 8.3 核心回测指标

| 指标 | 说明 | 目标 |
|------|------|------|
| **年化收益率** | 策略年度收益 | > 30% |
| **夏普比率** | 风险调整收益 | > 1.5 |
| **最大回撤** | 历史最大亏损 | < 20% |
| **胜率** | 盈利交易占比 | > 50% |
| **盈亏比** | 平均盈利/平均亏损 | > 1.5 |

---

## 9. 实施计划

### 阶段一：数据与事件定义（1周）

- [ ] 部署 Qlib 环境
- [ ] 下载 A 股日线数据
- [ ] 定义"涨停"事件标记逻辑
- [ ] 定义"二板"、"主升浪"事件
- [ ] 构建样本筛选管道

### 阶段二：特征因子构建（1周）

- [ ] 基础量价因子库
- [ ] 涨停相关特征
- [ ] 板块/情绪特征
- [ ] 因子有效性检验（IC 分析）
- [ ] 特征选择

### 阶段三：模型训练（1周）

- [ ] 数据集划分
- [ ] LightGBM 多分类训练
- [ ] 超参数调优
- [ ] 特征重要性分析

### 阶段四：回测验证（1周）

- [ ] 分年回测（2022/2023）
- [ ] 样本外检验
- [ ] 归因分析
- [ ] 核心因子确认

**预计总周期：4 周**

---

## 10. 进一步优化方向

### 10.1 Kronos 增强（后续阶段）

在 Qlib 因子验证有效后引入：

- 将 Qlib 有效因子作为 Kronos 输入
- 深度学习挖掘因子间非线性组合
- 融合传统因子与 AI 因子

### 10.2 因子方向扩展

| 方向 | 说明 |
|------|------|
| 分钟线因子 | 切换到 5 分钟线，捕捉盘中信号 |
| 基本面因子 | 市值、PE、PB 等 |
| 北向资金 | 沪深港通持股数据 |

---

## 11. 风险提示

| 风险类型 | 说明 | 应对 |
|----------|------|------|
| **过拟合** | 历史规律未必持续 | 多年份、多市场验证 |
| **流动性风险** | 小盘股难以买入 | 限制市值/成交量门槛 |
| **市场风格切换** | 策略有效性可能变化 | 定期模型更新 |
| **样本量稀疏** | 涨停股本身较少 | 扩大时间范围 |

---

*文档更新于 2026-05-11*
