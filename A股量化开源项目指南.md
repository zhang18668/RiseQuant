# A股量化开源项目指南

> 本文档介绍三个主流A股量化开源项目的定位、功能与组合使用方案，帮助你快速搭建因子挖掘到实盘交易的完整链路。

---

## 目录

1. [项目总览](#1-项目总览)
2. [vnpy —— 交易通道层](#2-vnpy--交易通道层)
3. [Kronos —— AI因子挖掘](#3-kronos--ai因子挖掘)
4. [Qlib —— 端到端量化投研平台](#4-qlib--端到端量化投研平台)
5. [技术栈组合方案](#5-技术栈组合方案)
6. [快速启动路线图](#6-快速启动路线图)
7. [数据流架构](#7-数据流架构)
8. [选型建议](#8-选型建议)

---

## 1. 项目总览

| 项目 | 语言 | 定位 | 成熟度 | 适合场景 |
|------|------|------|--------|----------|
| **vnpy** | Python | 量化交易框架 / 交易通道 | ★★★★★ | 连接策略与交易所，负责下单、风控、仓位管理 |
| **Kronos** | Python | AI因子挖掘 | ★★★☆☆ | 用深度学习自动发现有效因子，偏研究侧 |
| **Qlib** | Python | 端到端量化投研平台 | ★★★★★ | 覆盖因子、回测、模型、模拟全流程 |

---

## 2. vnpy —— 交易通道层

### 2.1 定位

vnpy 是国内最成熟的 **量化交易框架**，定位于"策略与交易所之间的通道层"。它不负责因子计算和模型预测，核心价值是把交易信号转化为真实的订单。

### 2.2 核心功能

- **多交易接口支持**：CTP（期货）、CTP证券、飞马、无限易等
- **品种覆盖**：期货、期权、股票、ETF、可转债
- **事件驱动架构**：适合策略开发和回测
- **风险控制**：内置仓位管理、交易限制等
- **策略模块**：CTA策略、自适应均线等基础策略模板

### 2.3 架构图

```
┌──────────────────────────────────────┐
│            策略层 (Strategy)          │
│    CTA策略 / 趋势跟踪 / 做市策略      │
├──────────────────────────────────────┤
│            引擎层 (Engine)            │
│  事件驱动引擎 / 缓存管理 / 日志       │
├──────────────────────────────────────┤
│            接口层 (Gateway)           │
│  CTP / CTP证券 / 飞马 / 无限易        │
├──────────────────────────────────────┤
│         交易所（期货/证券）           │
└──────────────────────────────────────┘
```

### 2.4 适用场景

- 连接策略信号与券商/期货公司交易通道
- 实现订单路由、仓位管理、风控逻辑
- **不擅长**：因子计算、机器学习模型

### 2.5 安装与资源

```bash
pip install vnpy
```

- GitHub: https://github.com/vnpy/vnpy
- 文档: https://www.vnpy.com/docs

---

## 3. Kronos —— AI因子挖掘

### 3.1 定位

Kronos 是一个基于 **深度学习的因子挖掘框架**，定位于研究侧。它的核心思路是：用神经网络自动从原始量价数据中发现传统指标难以捕捉的有效信号。

### 3.2 核心功能

- **自动因子挖掘**：输入原始特征，输出有效因子
- **深度学习模型**：利用神经网络学习市场模式
- **因子有效性评估**：内置评估指标筛选优质因子
- **Alpha因子库**：提供已挖掘的Alpha因子集合

### 3.3 工作流程

```
原始量价数据 (K线、成交量、订单簿)
         ↓
特征工程 + 因子候选集
         ↓
深度学习模型 (神经网络)
         ↓
有效因子输出
         ↓
因子入库与评估
```

### 3.4 适用场景

- 发现传统指标难以捕捉的隐蔽信号
- 利用AI能力挖掘未知因子
- 研究性质的因子探索

### 3.5 局限性

- **偏向研究侧**，不直接支持实盘交易
- 需要自行对接下游执行层（vnpy）
- 社区规模较小，文档相对薄弱

### 3.6 安装与资源

```bash
pip install kronos-q
```

- GitHub: https://github.com/shiyu-coder/Kronos

---

## 4. Qlib —— 端到端量化投研平台

### 4.1 定位

Qlib 是微软亚洲研究院开源的 **端到端量化投资平台**，覆盖从数据存储、因子计算、模型训练、回测验证到模拟交易的完整链路。是目前国内功能最完整、生态最活跃的量化开源项目。

### 4.2 核心功能

#### 4.2.1 数据存储 (QlibData)

| 特性 | 说明 |
|------|------|
| 高性能存储 | 支持日线、分钟线、tick级数据 |
| 格式优化 | 二进制格式，快速读取 |
| 数据丰富 | 包含基本面、行情、资金流等 |

#### 4.2.2 因子系统

- **内置因子库**：Alpha158、Alpha360等数百个因子
- **自定义因子**：支持用户编写Python表达式定义新因子
- **因子计算框架**：高性能批量计算

#### 4.2.3 模型训练

| 模型类型 | 支持情况 |
|----------|----------|
| 机器学习 | LightGBM、GBDT、XGBoost |
| 深度学习 | MLP、LSTM、Transformer、Graph Network |
| 时间序列 | 已有封装，可扩展 |

#### 4.2.4 回测框架

- **完整模拟撮合**：支持日线/分钟线回测
- **业绩归因**：收益、夏普、最大回撤等指标
- **分析工具**：IC分析、分组回测、累乘收益分析

#### 4.2.5 实盘对接

- **Qianfan**（vnpy云服务）
- **Quick** 等第三方通道
- 支持与vnpy配合使用

### 4.3 架构图

```
┌─────────────────────────────────────────────────────────┐
│                     应用层                                │
│   因子研究 / 模型训练 / 回测分析 / 因子挖掘               │
├─────────────────────────────────────────────────────────┤
│                    算法层                                 │
│   LightGBM / GBDT / LSTM / Transformer / GNN            │
├─────────────────────────────────────────────────────────┤
│                    因子层                                 │
│   Alpha158 / Alpha360 / 自定义因子 / Kronos因子          │
├─────────────────────────────────────────────────────────┤
│                    数据层                                 │
│              QlibData (日/分钟/tick)                     │
└─────────────────────────────────────────────────────────┘
```

### 4.4 适用场景

- 直接使用内置因子库快速验证想法
- 用机器学习训练预测模型
- 完整回测验证策略
- **已具备实盘对接能力**

### 4.5 安装与资源

```bash
pip install pyqlib
```

- GitHub: https://github.com/microsoft/qlib
- 文档: https://qlib.io

---

## 5. 技术栈组合方案

根据不同目标，推荐以下组合：

### 方案A：快速上线（推荐新手）

```
Qlib（主力） + vnpy（执行）
```

| 层级 | 工具 | 职责 |
|------|------|------|
| 因子 | Qlib 内置因子 | 直接使用 Alpha158 等成熟因子 |
| 模型 | Qlib LightGBM | 训练预测模型 |
| 回测 | Qlib 回测框架 | 验证策略 |
| 执行 | vnpy | 交易通道下单 |

**适合**：想快速验证想法、不想做太多底层开发

---

### 方案B：AI因子挖掘路线

```
Kronos + Qlib + vnpy
```

| 层级 | 工具 | 职责 |
|------|------|------|
| 因子挖掘 | Kronos | 深度学习自动发现因子 |
| 因子验证 | Qlib | 导入 Kronos 因子并检验有效性 |
| 回测 | Qlib | 策略回测 |
| 执行 | vnpy | 交易通道下单 |

**适合**：想挖掘独特因子、建立差异化策略

---

### 方案C：全链路自建

```
自建因子层 + Qlib 回测 + vnpy 执行
```

| 层级 | 工具 | 职责 |
|------|------|------|
| 因子计算 | 自建 / Kronos | 特征与因子计算 |
| 因子存储 | QlibData | 高性能数据存储 |
| 模型 | Qlib | 模型训练与预测 |
| 回测 | Qlib | 回测验证 |
| 执行 | vnpy | 实盘交易 |

**适合**：有开发能力，想要完全掌控各环节

---

## 6. 快速启动路线图

### 阶段一：环境搭建（1-2天）

```bash
# 安装核心依赖
pip install pyqlib vnpy

# 初始化 Qlib 数据
python -m qlib.run.get_data --target_dir ./qlib_data/cn_data
```

### 阶段二：因子与模型验证（3-7天）

```python
# Qlib 内置因子 + LightGBM 示例
from qlib.data.dataset import DatasetH
from qlib.data.dataset.handler import Alpha158
from qlib.contrib.model.gbdt import LGBModel

# 1. 加载 Alpha158 因子
handler = Alpha158(start_date="2018-01-01", end_date="2023-12-31")

# 2. 构建数据集
dataset = DatasetH(handler=handler)

# 3. 训练 LightGBM 模型
model = LGBModel()
model.fit(dataset)
```

### 阶段三：回测验证（1-3天）

```python
from qlib.workflow import R
from qlib.utils import init_instance_by_config
from qlib.contrib.evaluate import backtest_daily

# 运行回测
R.start(" Experiments / alpha_lgb")
# ... 回测配置 ...
report = backtest_daily(start_date="2023-01-01", end_date="2023-12-31")
```

### 阶段四：对接实盘（1-3天）

```
Qlib 信号输出 → vnpy API 下单
```

- 通过 Qlib 的 `Executor` 和 `Trader` 接口对接 vnpy
- 或自行编写信号处理器，将 Qlib 输出转化为 vnpy 订单

---

## 7. 数据流架构

```
原始行情数据
     │
     ▼
┌─────────────────────────────────────────────────────┐
│                   数据存储层                          │
│              QlibData（日/分钟/tick）                  │
└─────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────┐
│                   因子计算层                           │
│  ┌─────────────┐        ┌─────────────┐             │
│  │ Qlib 内置因子 │        │ Kronos 挖掘因子 │          │
│  │ Alpha158    │        │ 深度学习生成  │             │
│  │ Alpha360    │        │              │             │
│  └─────────────┘        └─────────────┘             │
└─────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────┐
│                   模型预测层                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ LightGBM │ │  LSTM   │ │Transformer│            │
│  └──────────┘ └──────────┘ └──────────┘            │
└─────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────┐
│                   信号生成层                           │
│           多头信号 / 空头信号 / 仓位权重                │
└─────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────┐
│                   执行层                              │
│  ┌──────────────────────────────────────────┐        │
│  │              vnpy 交易通道                 │        │
│  │  订单路由 / 风控 / 仓位管理 / 交易所接入   │        │
│  └──────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────┘
     │
     ▼
               交易所（期货/证券）
```

---

## 8. 选型建议

| 你的目标 | 推荐起点 | 理由 |
|----------|----------|------|
| **快速跑起来验证想法** | Qlib | 内置因子+回测，开箱即用，文档完善 |
| **想用AI挖掘独特因子** | Kronos | 深度学习因子发现，研究方向 |
| **需要对接实盘** | vnpy | 国内最成熟交易通道，社区活跃 |
| **完整量化投研流程** | Qlib + vnpy | Qlib负责研究，vnpy负责执行 |
| **想要AI + 全链路** | Kronos + Qlib + vnpy | 三者配合，覆盖因子到交易全流程 |

---

## 附录：常见问题

### Q1：这三个项目可以只用其中一个吗？

可以，但各有局限：

- **只用 vnpy**：缺少因子和模型能力，需要自己造轮子
- **只用 Kronos**：只做因子挖掘，不支持回测和实盘
- **只用 Qlib**：已较完整，可满足大部分需求

**推荐至少使用 Qlib + vnpy 的组合。**

### Q2：数据从哪里来？

| 来源 | 说明 |
|------|------|
| Qlib 内置数据下载 | `python -m qlib.run.get_data` |
| AKShare | 免费开源财经数据接口 |
| Tushare | 需要注册，有积分限制 |
| 聚宽 | 需要注册，有积分限制 |
| 券商数据接口 | CTP 等协议直连 |

### Q3：性能如何？

- **Qlib**：数据层做了二进制优化，读取速度快
- **vnpy**：事件驱动设计，订单处理延迟低
- **Kronos**：深度学习推理有一定延迟，适合低频

### Q4：学习曲线陡峭吗？

| 项目 | 学习曲线 | 文档质量 |
|------|----------|----------|
| vnpy | 中等 | 好（中文社区活跃） |
| Kronos | 较陡 | 一般 |
| Qlib | 中等 | 好（微软维护，英文文档完善） |

---

*文档更新于 2026-05-11*
