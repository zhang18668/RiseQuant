# 涨停二板主升浪因子挖掘项目方案

> 本文档聚焦一个具体的量化策略目标：**训练能够选出"首板涨停后、能够第二次涨停、随后走出主升浪"的股票**的因子模型。

---

## 1. 策略核心逻辑

### 1.1 目标形态

```
股价
  │
  │                                    ╱ ╲
  │                                  ╱     ╲
  │                               ╱         ╲    主升浪
  │                            ╱             ╲  ╱
  │                         ╱                 ╱
  │                      ╱                   ↑
  │                   ╱                  第二次涨停
  │                ╱                    （时间间隔 T 内）
  │             ╱                       ↑
  │          ╱                       ╱
  │       ╱                        ╱
  │    ╱                         ╱
  │ ──────────────────────────────
  │         ↑
  │    首板涨停
  │
└─────────────────────────────────────────────→ 时间
```

### 1.2 三个关键节点

| 节点 | 定义 | 技术处理 |
|------|------|----------|
| **首板涨停** | 日涨幅 ≥ 9.9%（ST股除外） | 事件标记 |
| **二板涨停** | 首板后 N 个交易日内再次涨停 | 事件序列检测 |
| **主升浪** | 二板后 M 个交易日内涨幅达到阈值 | 区间收益计算 |

### 1.3 核心问题

> **本质是一个事件序列预测问题**：在首板涨停发生后，预测未来能否出现二次涨停，以及二次涨停后是否能走出主升浪。

---

## 2. 方案选型

### 2.1 为什么选 Qlib 作为核心

| 需求 | Qlib 支持情况 |
|------|--------------|
| 自定义涨停事件因子 | ✅ 支持 Python 表达式因子 |
| 事件序列特征构建 | ✅ 可通过 `qlib.data.dataset.handler` 自定义 |
| 机器学习模型 | ✅ LightGBM / GBDT / LSTM 等 |
| 回测验证 | ✅ 内置回测框架 |
| 日线/分钟线数据 | ✅ 两者都支持 |

### 2.2 是否需要 Kronos

| 场景 | 是否需要 Kronos |
|------|----------------|
| 只做经典技术因子 | 不需要，Qlib 内置因子足够 |
| 想挖掘未知隐含因子 | 需要，Kronos 可补充 |
| 时间紧迫，先跑通逻辑 | 不需要，Qlib 更快 |

**建议：先基于 Qlib 自建因子体系跑通，验证有效后，再用 Kronos 做增强挖掘。**

### 2.3 vnpy 的角色

vnpy 负责 **信号到订单的转化**，在因子和模型验证有效后再对接。

---

## 3. 技术方案

### 3.1 整体架构

```
原始行情数据（AKShare / QlibData）
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                    Qlib 数据层                           │
│              日线数据（含复权因子）                       │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                  事件定义层                              │
│     涨停事件标记 / 事件序列构建 / 样本筛选                 │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                  特征因子层                              │
│    量价因子 / 资金流因子 / 板块因子 / 情绪因子            │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                  标签定义层                              │
│    二板成功标签 / 主升浪标签 / 复合标签                   │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                  模型训练层                              │
│              LightGBM 二分类 / 多分类                     │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                  回测验证层                              │
│            分年回测 / 样本外检验 / 归因分析               │
└─────────────────────────────────────────────────────────┘
         │
         ▼
         vnpy 实盘执行
```

---

## 4. 因子体系设计

### 4.1 样本定义

#### 正样本
首板涨停后 **5 个交易日**内出现二板，且二板后 **10 个交易日**涨幅超过 15%。

#### 负样本
首板涨停后 **5 个交易日内未出现二板**，或二板后未达到主升浪标准。

#### 观察样本
首板涨停后无论是否二板，用于提取特征。

### 4.2 特征分类

#### A. 原始量价特征（在首板收盘时提取）

```python
# 基础量价
feature_list = [
    "returns(close, 1)",      # 昨日收益率
    "returns(close, 3)",      # 3日收益率
    "returns(close, 5)",      # 5日收益率
    "returns(close, 10)",     # 10日收益率
    "volume / volume(rolling_5_mean)",  # 量比
    "close / close(rolling_20_min)",   # 相对20日均线位置
    "close / close(rolling_60_min)",   # 相对60日均线位置
    "std(returns(close, 5))",          # 5日波动率
    "std(returns(close, 10))",         # 10日波动率
]
```

#### B. 涨停相关特征

```python
# 涨停序列特征
limit_up_features = [
    # 首板当日特征
    "limit_up_amount",           # 涨停封单金额（手）
    "limit_up_duration",         # 涨停持续时间占比（分钟）
    "turnover_rate",             # 换手率
    "amplitude",                 # 振幅

    # 首板前特征
    "consecutive_up_days",       # 连续上涨天数
    "avg_volume_5d",             # 5日均量
    "volume_ratio",              # 首板当日量比 = 成交量 / 5日均量
    "pre_limit_up_leverage",     # 涨停前杠杆（融资融券余额变化）

    # 历史涨停统计
    "limit_up_count_20d",        # 20日内涨停次数
    "limit_up_count_60d",       # 60日内涨停次数
    "second_limit_up_ratio",    # 历史二板率（统计）
]
```

#### C. 板块与情绪特征

```python
sector_features = [
    "sector_leading_strength",   # 板块龙头强度
    "sector_avg_return",         # 板块平均涨幅
    "market_sentiment_index",    # 市场情绪指数（全市场涨跌比）
    "turnover_rate_market",      # 市场整体换手率
    "涨停股数量",                 # 当日涨停股数量（情绪指标）
]
```

#### D. 资金流特征

```python
money_flow_features = [
    "主力资金净流入",            # 超大单+大单净流入
    "主力资金流入占比",
    "北向资金净流入",            # if 可获取
    "融资融券余额变化",
]
```

### 4.3 标签定义

#### 标签1：二板预测（短期）

```python
# 首板后5日内是否出现二板
label_short = {
    "二板成功": 1,    # 5日内再次涨停
    "二板失败": 0,    # 5日内未二板
}
```

#### 标签2：主升浪预测（中期）

```python
# 二板后10日内涨幅是否超过15%
label_mid = {
    "主升浪成功": 1,  # 二板后10日涨幅 > 15%
    "主升浪失败": 0,  # 未达到主升浪标准
}
```

#### 复合标签（推荐）

```python
# 综合标签
label_combined = {
    "完美标的": 2,     # 二板成功 且 主升浪成功
    "二板成功": 1,     # 二板成功 但 主升浪失败
    "二板失败": 0,     # 未二板
}
```

---

## 5. 数据获取方案

### 5.1 数据来源

| 来源 | 说明 | 获取方式 |
|------|------|----------|
| **QlibData** | 内置日线数据 | `python -m qlib.run.get_data` |
| **AKShare** | 免费财经数据 | `pip install akshare` |
| **Tushare** | 需要积分 | 注册获取 token |

### 5.2 最小数据集

如果只需要验证策略，至少需要以下字段：

```python
required_fields = [
    "date",           # 日期
    "code",           # 股票代码
    "open",           # 开盘价
    "high",           # 最高价
    "low",            # 最低价
    "close",          # 收盘价
    "volume",         # 成交量
    "turnover_rate",  # 换手率
    "change_pct",     # 涨跌幅
]
```

### 5.3 Qlib 数据初始化

```bash
# 下载 A 股日线数据
python -m qlib.run.get_data --target_dir ./qlib_data/cn_data --region cn
```

---

## 6. 模型训练方案

### 6.1 训练流程

```python
from qlib.data.dataset import DatasetH
from qlib.contrib.model.xgboost import XGBModel
from qlib.data.dataset.handler import Alpha158

# Step 1: 定义样本筛选器（只选首板发生的股票）
class LimitUpSampleFilter:
    def __init__(self, n_days=5):
        self.n_days = n_days

    def filter(self, instrument, start_time, end_time):
        # 筛选逻辑：选出首板涨停的股票
        pass

# Step 2: 特征因子注册
from qlib.data.expression import Expression

# Step 3: 构建数据集
handler = Alpha158(start_date="2018-01-01", end_date="2023-12-31")
dataset = DatasetH(handler=handler, segments={
    "train": ("2018-01-01", "2020-12-31"),
    "valid": ("2021-01-01", "2021-12-31"),
    "test":  ("2022-01-01", "2023-12-31"),
})

# Step 4: 训练 LightGBM
model = LGBModel()
model.fit(dataset)
```

### 6.2 模型选择

| 模型 | 适用场景 | 推荐理由 |
|------|----------|----------|
| **LightGBM** | 首选 | 速度快，精度高，适合日线级别 |
| **XGBoost** | 备选 | 稳定性好，可与 LightGBM 交叉验证 |
| **LSTM** | 进阶 | 可捕捉时序依赖，但训练较慢 |
| **CatBoost** | 备选 | 对类别特征友好 |

### 6.3 评估指标

```python
metrics = {
    "IC (Information Coefficient)",  # 预测相关性
    "IR (Information Ratio)",         # IC均值/IC标准差
    "AUC",                            # 二分类区分度
    "Accuracy",                       # 准确率
    "Precision",                      # 精确率
    "Recall",                         # 召回率
}
```

---

## 7. 回测验证方案

### 7.1 模拟撮合规则

| 项目 | 规则 |
|------|------|
| 买入时机 | 首板次日开盘买入（模拟涨停后次日接力） |
| 卖出时机 | 二板次日开盘 / 固定 N 日后 / 止损线 |
| 仓位 | 单只股票仓位上限 20% |
| 滑点 | 0.5%（保守估计） |

### 7.2 回测框架

```python
from qlib.contrib.evaluate import backtest_daily

# 基于 Qlib 内置回测
backtest_config = {
    "start_date": "2022-01-01",
    "end_date": "2023-12-31",
    "account": 10000000,      # 1000万模拟账户
    "benchmark": "SH000300",  # 沪深300基准
    "strategy": {
        "topk": 10,           # 持仓最多10只
        "Sell_n": 5,          # 卖出持仓超过5日的
        "signal_method": "model_output",  # 使用模型信号
    }
}

report = backtest_daily(**backtest_config)
```

### 7.3 关键指标

| 指标 | 说明 | 目标 |
|------|------|------|
| 年化收益率 | 策略年度收益 | > 30% |
| 夏普比率 | 风险调整收益 | > 1.5 |
| 最大回撤 | 历史最大亏损 | < 20% |
| 胜率 | 盈利交易占比 | > 50% |
| 盈亏比 | 平均盈利/平均亏损 | > 1.5 |

---

## 8. 项目实施计划

### 阶段一：数据与事件定义（1-2周）

- [ ] 部署 Qlib 环境
- [ ] 下载/接入 A 股日线数据
- [ ] 定义"涨停"事件标记逻辑
- [ ] 定义"二板"、"主升浪"事件
- [ ] 构建样本筛选管道

### 阶段二：特征因子构建（1-2周）

- [ ] 构建基础量价因子库
- [ ] 构建涨停相关特征
- [ ] 构建板块/情绪特征（可选）
- [ ] 因子有效性检验（IC 分析）
- [ ] 特征选择与降维

### 阶段三：模型训练（1-2周）

- [ ] 数据集划分（训练/验证/测试）
- [ ] 模型选择与训练
- [ ] 超参数调优
- [ ] 模型评估与选择
- [ ] 特征重要性分析

### 阶段四：回测验证（1周）

- [ ] 模拟撮合回测
- [ ] 分年回测（2022/2023）
- [ ] 样本外检验
- [ ] 归因分析
- [ ] 参数调优

### 阶段五：实盘对接（1周）

- [ ] vnpy 通道对接
- [ ] 信号到订单转化逻辑
- [ ] 风控模块
- [ ] 纸盘测试
- [ ] 实盘切换

---

## 9. 风险提示

| 风险类型 | 说明 | 应对 |
|----------|------|------|
| **过拟合** | 涨停股历史规律未必持续 | 多市场、多年份验证 |
| **流动性风险** | 小盘股难以买入 | 限制市值/成交量门槛 |
| **市场风格切换** | 策略有效性可能随市场变化 | 定期模型更新 |
| **涨停板制度风险** | A股涨跌停限制 | 考虑封板时间、封单量 |
| **交易滑点** | 实际成交价与信号价差 | 保守滑点估计 |

---

## 10. 进一步优化方向

### 10.1 Kronos 增强挖掘

在 Qlib 因子验证有效后，可引入 Kronos 进行深度学习因子增强：

- 将 Qlib 输出的有效因子作为输入
- 用 Kronos 挖掘因子间非线性组合
- 融合传统因子与 AI 因子

### 10.2 分钟线级别

当前方案基于日线，适合中低频。如果想捕捉更精细的买点：

- 切换到 5 分钟线数据
- 用 vnpy 的 CTA 模块做趋势跟随
- 引入盘口数据（Level2）

### 10.3 基本面增强

| 因子 | 来源 |
|------|------|
| 市值因子 | Tushare / Qlib |
| 估值因子 (PE/PB) | AKShare |
| 业绩预告 | 财报数据 |

---

*文档更新于 2026-05-11*
