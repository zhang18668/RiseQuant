# 量化选股系统 v3.0 - 后端依赖
# ── 基础 ──
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
pandas>=2.0.0
numpy>=1.24.0

# ── 数据库 ──
sqlalchemy>=2.0.0
pymysql>=1.1.0

# ── 数据源 (决议D2: TDX主 → akshare备 → baostock兜底) ──
mootdx>=0.9.0
akshare>=1.10.0
baostock>=0.8.8

# ── 模型 (决议D3: LightGBM主模型 + 小MLP辅模型) ──
lightgbm>=4.0.0
scikit-learn>=1.3.0
# torch  # 小MLP辅模型, 按需安装
